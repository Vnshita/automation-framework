services:
  selenium:
    image: selenium/standalone-chrome:4.20.0
    ports:
      - "4444:4444"

  automation:
    build: .
    depends_on:
      - selenium
    command: >
      sh -c "sleep 10 && pytest --alluredir=allure-results"